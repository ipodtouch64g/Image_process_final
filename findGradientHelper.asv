for i=1:7
    avgGradient = zeros(720,1280,3);
    for j=204*(i-1)+1:204*i
        avgGrandient(:,:,1) = avgGradient(:,:,1) + imgradient(myVideo(:,:,1,j));
        avgGrandient(:,:,2) = avgGradient(:,:,2) + imgradient(myVideo(:,:,2,j));
        avgGrandient(:,:,3) = avgGradient(:,:,3) + imgradient(myVideo(:,:,3,j));
    end
    
end

